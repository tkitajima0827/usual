DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/yozan_kanri?schema=public"

# セッション署名用の秘密鍵。`openssl rand -base64 32` 等で生成した値を設定する。
# 本番環境では必須（未設定だと起動時にエラーになる）。開発環境では未設定でも
# 固定のダミー値で動作するが、必ず設定することを推奨する。
SESSION_SECRET=""
